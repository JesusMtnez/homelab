# Nix
result
.direnv

# k8s
kubeconfig

# project
**/env.secrets

# archived legacy cluster manifests (kept on disk, untracked)
kubernetes/fluffy.backup/

# node
node_modules/

# secrets (sops and age)
.decrypted~*
*.secret.env
*.secret.yml
*.agekey
*.pub
*.key
*.pem
*.age

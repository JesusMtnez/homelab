---
# yaml-language-server: $schema=https://taskfile.dev/schema.json
version: "3"

tasks:
  build:
    desc: Build documentation site
    cmds:
      - nix build .#site

  serve:
    desc: Serve documentation site locally
    cmds:
      - nix develop .#site --command docsify serve docs
